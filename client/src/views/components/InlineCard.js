import styled from "styled-components";
import { space, layout, color, border, shadow, position } from "styled-system";

const InlineCard = styled.span`
  ${space};
  ${layout};
  ${color};
  ${border};
  ${shadow};
  ${position};
`;

export default InlineCard;
